Готово. Новый план записан в /home/user/.kimi/plans/amadeus-cho-john-stewart-huntress.md.

Ключевые пункты:

Системный systemd unit — перенести из ~/.config/systemd/user/ в /etc/systemd/system/, чтобы Ollama запускалась и логи шли в /var/log/ai-stack.log. Требуется sudo.

INST08_MCP_QUERY_1C.md — полный гид развёртывания MCP_QUERY_1C по образцу INST06.

Укрепление Dockerfile'ов — непривилегированный пользователь sdql, healthcheck, labels, ограничения ресурсов.

Улучшение docker-compose — healthcheck, limits, read-only fs, tmpfs.

Скрипт инициализации volumes — ai-init-sdql-volumes.sh для подготовки прав.

Обновление скиллов — ubuntu-admin и java-docker-publish.

Тесты MCP_QUERY_1C — McpQuery1cToolsTest.java.

Коммиты — 5 коммитов в develop с Refs Redmine #646.

## Статус выполнения (2026-06-06)

- ✅ Системный systemd unit — создан /etc/systemd/system/ai-stack.service, enabled
- ✅ INST08_MCP_QUERY_1C.md — создан и закоммичен
- ✅ Dockerfile'ы — USER sdql, wget, healthcheck (127.0.0.1)
- ✅ docker-compose.yml — healthcheck (CMD-SHELL), deploy.resources.limits, tmpfs
- ✅ ai-init-sdql-volumes.sh — создан, исполняемый
- ✅ Скилл ubuntu-admin — обновлён (ai-sdql-mcp.md, ai-mcp-query-1c.md, ai-autostart.md, ai-init-sdql-volumes.sh)
- ✅ Скилл java-docker-publish — обновлён (non-root user, healthcheck, resources, read-only fs)
- ✅ McpQuery1cToolsTest — 12 тестов, все проходят
- ✅ Коммиты — 4 коммита в develop, запушены
- ⚠️ MCP зависание при подключении к Kimi — добавлен initialize handshake и SSE transport, но требуется persistent SSE connection (отдельная задача)
